/*
    Prototype  (Prototipo = aquilo que foi criado antes)

    *prototype-based language   (linguagem baseada em prototipos)
    *prototype chain            (cadeia de prototipos)
    *__proto__

*/


// Quando criamos um objeto automaticamente ele herda diversos metodos e funcionalidades (prototipos)

//encapsulado, vão ter o __proto__ e herdar toda a cadeia que vem antes. 